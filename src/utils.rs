use bevy::prelude::*;

// a bunch of utility functions; can be split into submodules later

// check if a point is inside the polygon formed by points
pub fn point_inside_polygon(point: &Vec2, polygon: &Vec<Vec2>) -> bool {
    return false;
}